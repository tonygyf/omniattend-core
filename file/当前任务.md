4.27 落地：Workers 去 ONNX 化 + HuggingFace 人脸特征中心接入（已完成）
omniattend-core + mobilefacenet-server
本轮完成：

worker.ts：
移除 onnxruntime-node、sharp 本地推理链路；
新增外部推理调用：通过 FaceInferenceService（D1）或环境变量配置，调用 HuggingFace /embed/url；
/api/face/model/status 改为检测外部推理中心 /health；
批量注册默认改为“头像 URL → 外部推理 → 存 FaceEmbedding”，Workers 仅保留余弦相似度比对。

schema.sql：
新增 FaceInferenceService 配置表（baseUrl、apiToken、timeoutMs、modelVer、isActive）；
增加默认配置插入、索引与更新时间触发器。

mobilefacenet-server：
新增可直接部署到 HuggingFace Space 的 Docker 服务（app.py、Dockerfile、requirements.txt、README.md）；
提供 /health、/embed/url、/embed/base64 接口。

文档同步更新：
mobilefacenet-server/迁移文档.md
file/重新迁移.md 顶部状态说明


待执行：

卸载 Workers 不再使用的人脸推理依赖并刷新 lockfile。（已完成）
执行 npx tsc --noEmit 与 npm run build 复验。（已完成）


4.26 修正：人脸特征中心模型状态文案统一 ONNX（已完成）
omniattend-core
本轮完成：

services/dataService.ts：修正演示账号 fetchFaceModelStatus 兜底路径统一为 /models/mobilefacenet.onnx，补充 source: 'NONE'。
pages/AiInsights.tsx：顶部文案补充默认模型说明，按钮文案更新为“检测ONNX模型可用性”，状态区新增 message 展示。

构建验证： npx tsc --noEmit 与 npm run build 均通过。

4.25 落地：人脸特征中心按钮校验与验证参数输入优化（已完成）
omniattend-core
本轮完成：

pages/AiInsights.tsx 增强按钮逻辑：模型版本非空校验、模型不可用时前置拦截、阈值与最大人数范围合法性校验。
批量验证新增 probes JSON 输入框 + 前端 JSON 解析与结构校验。
帮助文案同步更新。

构建验证： 通过。

4.24 落地：Worker 接入 MobileFaceNet ONNX 真推理（已完成）
omniattend-core
本轮完成：

安装 onnxruntime-node、sharp 等依赖。
worker.ts 实现单例 ONNX Session、图像预处理（112×112×3 → float32）、真实特征提取（节点 img_inputs:0 → embeddings:0）。
POST /api/face/jobs/enroll-batch 支持头像驱动提取，相似度改为归一化向量点积，默认阈值调整为 0.55。

构建验证： 通过。

4.23 落地：人脸批量提取接入“学生头像驱动”真实链路（已完成）
omniattend-core
本轮完成：

POST /api/face/jobs/enroll-batch 改造为“优先样本 → 默认读 Student.avatarUri”模式。
新增 readAvatarBytes、deriveEmbeddingFromImageBytes、estimateImageQuality。
补充 AVATAR_NOT_SET / AVATAR_READ_FAILED 错误码。

构建验证： 通过。

4.22 落地：人脸特征中心去 mock 化（已完成）
omniattend-core
本轮完成：

默认模型版本改为 mobilefacenet_float32.tflite。
类型与 Worker 批处理接口对齐真实向量逻辑，移除 simulated 字段。
补充维度/探针缺失等错误保护。

构建验证： 通过。

4.21 落地：头像上传链路云端对齐（教师/学生）+ D1 回写（已完成）
omniattend-core
本轮完成：

统一 normalizeAvatarKey + slugifyName 规则。
新增 POST /api/student/profile/avatar，支持自动生成 key 并回写 Student.avatarUri。
教师头像链路同步对齐。

上线注意： 部署 Worker 后需回归已有头像覆盖、无头像首传等场景。

4.20 规划：人脸识别迁移至云端计算全流程（仅规划）
omniattend-core
详细规划了从端侧提取 → 云端提取/比对/判定的完整迁移路径，分 P0~P3 阶段，明确了协议冻结、服务分层、灰度回滚等策略。本轮不改代码。

4.19 ~ 4.8 主要变更（精炼）

Dashboard 统计口径优化：支持日/月/年/总切换，修复时间解析（1970 问题）、教师隔离、到勤率计算等。
人脸签到约束接入：CheckinTask 新增 faceRequired / faceMinScore，发布弹窗支持配置。
CheckinSubmission 增列：支持 photoKey、photoUri、faceVerifyScore、faceVerifyPassed（含迁移脚本）。
前端优化：路由懒加载 + chunk 拆分，显著降低首包体积。


4.7 ~ 4.3 补充记录（精炼）

人脸识别上云兼容改造（无 SQL 变更版）：新增照片上传接口，submitCheckin 支持可选人脸字段。
发布考勤任务弹窗班级列表修复：改为调用真实 /api/classrooms，移除失败回退 mock，避免显示历史班级名。
任务详情页补齐“发布后可见”的签到方式信息（位置、手势、密码）。