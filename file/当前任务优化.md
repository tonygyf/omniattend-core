# 当前任务优化（实时）

更新时间：2026-04-07

## 任务目标（已校正）
- 保持 `AiInsights` 页面 UI 骨架不变，改成“管理用户群人脸特征向量”的网页。
- 支持两类核心能力：
  - 批量提取人脸特征向量。
  - 批量测试模型在网页侧是否可用（阈值、通过率、均分）。
- 签到主流程不变：仍通过考勤任务发布与提交（`CheckinTask` + `CheckinSubmission`）。
- 数据库不做变更，沿用当前 `schema.sql`。

## 已完成（P0）

### 1) AiInsights 页面迁移完成（UI 骨架保留）
- 文件：`d:\typer\android_demo\omniattend-core\pages\AiInsights.tsx`
- 保留了原有结构：头部、主卡片、风险卡、学生/班级双视图表格、帮助弹窗。
- 数据语义已切换为人脸向量中心：
  - 顶部主卡片：班级筛选 + 模型版本 + 阈值 + 批量人数 + 两个按钮（批量提取/批量测试）。
  - 风险卡片：展示“无模板或低质量模板”的学生样本。
  - 学生视图：模板数、质量、模型版本、更新时间。
  - 班级视图：总人数、已建模人数、低质量模板数、平均质量。

### 2) 后端接口已补齐（不改数据库）
- 文件：`d:\typer\android_demo\omniattend-core\worker.ts`
- 新增接口：
  - `GET /api/face/templates/summary`
    - 聚合 `Student + Classroom + FaceEmbedding`，返回学生模板概览。
  - `POST /api/face/jobs/enroll-batch`
    - P0 先用 mock 向量批量写入 `FaceEmbedding`，验证云端批量流程可用。
  - `POST /api/face/jobs/verify-batch`
    - P0 基于现有模板质量和阈值输出测试结果（score/passed/avgScore）。
- 说明：以上接口均基于现有表实现，未引入新表。

### 3) 前端服务封装已完成
- 文件：`d:\typer\android_demo\omniattend-core\services\dataService.ts`
- 新增调用：
  - `fetchFaceTemplateSummary`
  - `enrollFaceBatch`
  - `verifyFaceBatch`

### 4) 类型定义已补齐
- 文件：`d:\typer\android_demo\omniattend-core\types.ts`
- 新增类型：
  - `FaceTemplateSummaryItem`
  - `FaceEnrollBatchResult`
  - `FaceVerifyBatchDetail`
  - `FaceVerifyBatchResult`

### 5) 模型文件已放到云端静态目录
- 来源模型：`d:\typer\android_demo\FaceCheck\app\src\main\assets\models\new\mobilefacenet_float32.tflite`
- 已复制到：
  - `d:\typer\android_demo\omniattend-core\public\models\mobilefacenet_float32.tflite`
- 部署后访问路径：
  - `/models/mobilefacenet_float32.tflite`

## 当前状态
- `schema.sql` 不变（按你的要求，不做数据库改动）。
- 签到流程不变（仍走 `CheckinTask` 发布、`submit` 提交，并使用 `faceRequired/faceMinScore`）。
- 构建状态：已通过 `npm run build`。

## P1 已完成
- 新增模型文件在线检测接口：
  - `GET /api/face/model/status`
  - 检测 `public/models/mobilefacenet_float32.tflite` 是否可通过静态资源访问。
- 批量提取接口升级：
  - `POST /api/face/jobs/enroll-batch` 支持外部 `samples[{studentId, vector, quality}]` 入参。
  - 有样本时按真实向量写库；无样本时才走模拟向量兜底。
- 批量测试接口升级：
  - `POST /api/face/jobs/verify-batch` 支持 `probes[{studentId, vector}]`。
  - 有探针向量时走余弦相似度（`COSINE`）；无探针时走质量模拟（`QUALITY_SIM`）。
  - 返回 `simulated` 字段，前端可明确展示当前测试模式。
- 页面补充模型检测入口：
  - `AiInsights` 新增“检测模型文件”按钮和状态展示（可用/不可用 + HTTP 状态码）。

## 当前结论
- 你要求的“不拆、直接做完”已完成：页面、接口、模型文件路径与文档都已同步。
- 数据库未改动，签到链路未改动（`CheckinTask + CheckinSubmission`）。
